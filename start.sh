#!/usr/bin/env bash

echo "=============================================="
echo "  Запуск локального сервера D&D..."
echo "=============================================="

# Перейти в папку, где лежит сам скрипт (на случай, если запуск из другого места)
cd "$(dirname "$0")" || exit 1

# Запустить сервер
python3 server.py

# Если сервер упал с ошибкой — не закрывать окно сразу
read -p "Нажмите Enter для выхода..."
